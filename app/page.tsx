import { Hero } from "@/components/Hero";

export default function Home() {
  return (
    <main>
      <Hero />
      {/* Next sections (About, Projects, etc.) will be composed here
          one at a time, per our iteration workflow. */}
    </main>
  );
}
